# Use the same randomly generated value when expanding OPENCLAW_HOOK_TOKEN in
# openclaw-config-set.json. Store the real file on the VM with mode 0600.
OPENCLAW_HOOK_TOKEN=replace-with-random-hook-token
